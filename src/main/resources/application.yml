spring:
  application:
    name: Spring Conventional Workshop
  datasource:
    url: jdbc:h2:mem:conventional
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
